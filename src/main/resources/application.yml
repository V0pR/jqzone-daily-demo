#datasource
datasource:
  url: jdbc:mysql://localhost:3306/wangjq_core?serverTimezone=UTC&useLegacyDatetimeCode=false&characterEncoding=UTF8&rewriteBatchedStatements=true&zeroDateTimeBehavior=convertToNull&allowMultiQueries=true
  username: root
  password: 123456
  driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 8080

logging:
  file:
    path: ./logs

#ebean
ebean:
  db:
    ddl:
      generate: false
      run:  false
    search:
      packages: com.core.domain
  migration:
    run: true

# security
spring:
  security:
    user:
      password: admin
      name: admin
#  http:
#    encoding:
#      charset: utf-8
#      force: true
#      enabled: true
#  messages:
#    encoding: UTF-8
